upstream p2p_transmission_api {
    server 127.0.0.1:3333;
    keepalive 16;
}

upstream p2p_transmission_web {
    server 127.0.0.1:8081;
    keepalive 16;
}

server {
    listen 80;
    listen [::]:80;
    server_name p2p.yxswy.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/p2p-acme;
    }

    location = /health {
        proxy_pass http://p2p_transmission_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location ^~ /v1/ {
        proxy_pass http://p2p_transmission_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_buffering off;
        proxy_read_timeout 75s;
    }

    location / {
        proxy_pass http://p2p_transmission_web;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name turn.p2p.yxswy.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/p2p-acme;
    }

    location / {
        return 404;
    }
}
